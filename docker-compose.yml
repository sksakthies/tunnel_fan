services:
  backend:
    build: ./project_final
    container_name: tunnel_backend
    ports:
      - "5000:5000"
    environment:
      - TZ=Asia/Kolkata

  frontend:
    build: ./fan-dashboard
    container_name: tunnel_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend